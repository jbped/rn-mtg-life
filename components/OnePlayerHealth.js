import React from 'react';
import { View, Text } from 'react-native';

export default function OnePlayerHealth({ navigation }) {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>One Player Health Menu</Text>
    </View>
  );
}
